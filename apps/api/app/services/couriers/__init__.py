"""The three couriers and the layer that chooses between them.

A `*_service` here decides *when* to book, cancel or advance; the matching
`providers/*_provider.py` speaks the courier's own protocol."""
